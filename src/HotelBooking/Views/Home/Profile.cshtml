@*@model List<Hotel>*@

@{
    ViewBag.Title = "Profile";
}



<div class="container">
    <h1>Name: </h1>

</div>







@*<div class="container">
    <h1>My Booked Hotels</h1>
    @if (Model.Count == 0)
    {
        <p>You have not booked any hotels yet.</p>
    }
    else
    {
        <table class="table">
            <thead>
                <tr>
                    <th>Hotel Name</th>
                    <th>Check-in Date</th>
                    <th>Check-out Date</th>
                    <th>Number of Guests</th>
                    <th>Total Price</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var hotel in Model)
                {
                    <tr>
                        <td>@hotel.Name</td>
                        <td>@hotel.CheckInDate.ToShortDateString()</td>
                        <td>@hotel.CheckOutDate.ToShortDateString()</td>
                        <td>@hotel.NumberOfGuests</td>
                        <td>@hotel.TotalPrice.ToString("C")</td>
                    </tr>
                }
            </tbody>
        </table>
    }
</div>*@







